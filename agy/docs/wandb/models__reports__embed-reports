---
source_url: https://docs.wandb.ai/models/reports/embed-reports
---

[Guides](/models/track)

[Reports](/models/reports)

# Embed a report

Embed W&B reports directly into Notion or with an HTML `iframe` element.

Embed a W&B report in an external page or tool so that your team can view live experiment results alongside other documentation. This page describes how to generate an embed code from a report and use it in HTML, Confluence, Notion, or Gradio.

## 

​

HTML `iframe` element

Use an HTML `iframe` element to embed a report on any web page that accepts custom HTML. W&B provides a prebuilt embed code that you can copy directly from the report. In the upper right of a report, select the **Share** button to open a dialog, then select **Copy embed code**. The copied snippet is an `iframe` HTML element that you can paste into any page that accepts custom HTML.

Only _public_ reports are viewable when embedded.

![Getting embed code](https://mintcdn.com/wb-21fd5541/uqPGOvf46GQ1vVUB/images/reports/get_embed_url.gif?s=e05c89e84294fac3458371de33fd969f)

## 

​

Confluence

Confluence supports embedded reports through its built-in `iframe` macro. The following animation demonstrates how to insert the direct link to the report within an `iframe` cell in Confluence.

![Embedding in Confluence](https://mintcdn.com/wb-21fd5541/uqPGOvf46GQ1vVUB/images/reports/embed_iframe_confluence.gif?s=57f7b54f8a23f04c627e7e80ca2503f6)

## 

​

Notion

Notion’s **Embed** block accepts the same embed code that W&B provides. The following animation demonstrates how to insert a report into a Notion document using an **Embed** block in Notion and the report’s embed code.

![Embedding in Notion](https://mintcdn.com/wb-21fd5541/uqPGOvf46GQ1vVUB/images/reports/embed_iframe_notion.gif?s=7ab3667a21ca039bcfbb195b929d3050)

## 

​

Gradio

To embed a W&B report in a Gradio app, including an app hosted on Hugging Face Spaces, use Gradio’s `gr.HTML` element to render an `iframe` that points to the report URL.
    
    
    import gradio as gr
    
    
    def wandb_report(url):
        iframe = f'<iframe src={url} style="border:none;height:1024px;width:100%">'
        return gr.HTML(iframe)
    
    
    with gr.Blocks() as demo:
        report = wandb_report(
            "https://wandb.ai/_scott/pytorch-sweeps-demo/reports/loss-22-10-07-16-00-17---VmlldzoyNzU2NzAx"
        )
    demo.launch()
    

Was this page helpful?

[Suggest edits](https://github.com/wandb/docs/edit/main/models/reports/embed-reports.mdx)[Raise issue](https://github.com/wandb/docs/issues/new?title=Issue on docs&body=Path: /models/reports/embed-reports)

[Clone and export reportsPrevious](/models/reports/clone-and-export-reports)[Compare runs across projectsNext](/models/reports/cross-project-reports)

⌘I
